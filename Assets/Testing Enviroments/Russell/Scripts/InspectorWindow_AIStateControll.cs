using System;
using System.Collections;
using System.Collections.Generic;
using UnityEditor;
using UnityEngine;

namespace Harry
{
 /*   public class InspectorWindow_AIStateControll : EditorWindow
    {
        public delegate void ChangeToFindFlowerState();

        public static ChangeToFindFlowerState GoFindFlowers;
        [MenuItem("AI/ChangeState")]
        public static void ShowWindow()
        {
            GetWindow(typeof(InspectorWindow_AIStateControll));
        }
        // Start is called before the first frame update
        private void OnGUI()
        {
            if(GUILayout.Button("Change Bees to Find a flower"))
            {
                if (!Application.isPlaying)
                {
                    Debug.LogWarning("Editor must be playing for this button to function");

                }
                else GoFindFlowers.Invoke();
            }
            
            if(GUILayout.Button("Bee Relist"))
            {
                if (!Application.isPlaying)
                {
                    Debug.LogWarning("Editor must be playing for this button to function");

                }
                else GoFindFlowers.Invoke();
            }
            
            
        }
    }
    */

}
